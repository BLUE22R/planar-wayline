/**
 * 功能名称：键盘绑定占位（面状独立工程）
 * 说明：面状编辑不依赖航点飞控快捷键，仅保留输入框聚焦时的开关接口。
 */

/**
 * 开关航线键盘飞控（独立工程为空操作）
 * @param _isOpen 是否开启
 */
export const keyBindingSwitch = (_isOpen: boolean) => {};

/**
 * 更新控制面板高度占位（planarLine 引用）
 * @param _height 高度
 */
export const updateControlPanelHeight = (_height?: number) => {};
